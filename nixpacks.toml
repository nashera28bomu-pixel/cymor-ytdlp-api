[phases.setup]
nixPkgs = ["python311", "ffmpeg", "nodejs_18"]

[phases.install]
cmds = [
  "pip install --no-cache-dir yt-dlp",
  "npm install"
]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "node server.js"
